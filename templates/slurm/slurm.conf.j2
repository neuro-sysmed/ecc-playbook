AccountingStorageType=accounting_storage/slurmdbd
AuthType=auth/munge
ClusterName=nsm
ControlMachine=ecc-head
DefMemPerCPU=1000
JobAcctGatherType=jobacct_gather/cgroup
ProctrackType=proctrack/cgroup
SchedulerType=sched/backfill
SelectType=select/cons_res
SelectTypeParameters=CR_Core_Memory
SlurmctldLogFile=/var/log/slurm/slurmctld.log
SlurmctldPort=6817
SlurmdDebug=3
SlurmdLogFile=/var/log/slurm/slurmd.log
SlurmdPort=6818
SlurmdSpoolDir=/var/spool/slurm/slurmd
SlurmUser=slurm
StateSaveLocation=/var/lib/slurm/slurmctld
TaskPlugin=task/cgroup


{% for i in small|default([]) %}
NodeName={{ i.name }}{% for k in i | sort if k != 'name' %} {{ k }}={{ 'YES' if i[k] is sameas true else ('NO' if i[k] is sameas false else i[k]) }}{% endfor %}

{% endfor %}
NodeName=ecc-small-0 CoresPerSocket=1 CPUs=32 RealMemory=7962 Sockets=1 ThreadsPerCore=1

{% for i in medium|default([]) %}
NodeName={{ i.name }}{% for k in i | sort if k != 'name' %} {{ k }}={{ 'YES' if i[k] is sameas true else ('NO' if i[k] is sameas false else i[k]) }}{% endfor %}

{% endfor %}
NodeName=ecc-medium-0 CoresPerSocket=1 CPUs=32 RealMemory=7962 Sockets=1 ThreadsPerCore=1

{% for i in large|default([]) %}
NodeName={{ i.name }}{% for k in i | sort if k != 'name' %} {{ k }}={{ 'YES' if i[k] is sameas true else ('NO' if i[k] is sameas false else i[k]) }}{% endfor %}

{% endfor %}
NodeName=ecc-large-0 CoresPerSocket=1 CPUs=32 RealMemory=7962 Sockets=1 ThreadsPerCore=2

{% for i in huge|default([]) %}
NodeName={{ i.name }}{% for k in i | sort if k != 'name' %} {{ k }}={{ 'YES' if i[k] is sameas true else ('NO' if i[k] is sameas false else i[k]) }}{% endfor %}

{% endfor %}
NodeName=ecc-huge-0 CoresPerSocket=1 CPUs=32 RealMemory=7962 Sockets=1 ThreadsPerCore=1


# Partition
PartitionName=small State=UP MaxTime=UNLIMITED Default=YES Nodes={{ groups[ 'small' ]|default(['ecc-small-0']) | join(',') }}
PartitionName=medium State=UP MaxTime=UNLIMITED Default=NO Nodes={{ groups[ 'medium' ]|default(['ecc-medium-0']) | join(',') }}
PartitionName=large State=UP MaxTime=UNLIMITED Default=NO Nodes={{ groups[ 'large' ]|default(['ecc-large-0']) | join(',') }}
PartitionName=huge State=UP MaxTime=UNLIMITED Default=NO Nodes={{ groups[ 'huge' ]|default(['ecc-huge-0']) | join(',') }}


